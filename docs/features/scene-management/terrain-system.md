# Система террейнов (ландшафтных слоев)

> **Основа трёхмерных сцен** — система создания и управления террейнами в Qryleth для построения реалистичных ландшафтов любой сложности.

---

## 🌍 Что такое система террейнов?

**Система террейнов** в Qryleth — это комплексное решение для создания и управления рельефом местности в 3D-сценах. Она позволяет создавать как простые плоские поверхности, так и сложные ландшафты с горами, долинами, холмами и другими географическими формами.

### Основные компоненты системы:

- **Базовый террейн** — основа ландшафта, создаваемая с помощью алгоритмов шума Perlin или карт высот
- **Процедурные операции** — холмы, долины, кратеры, плато для детализации рельефа  
- **Слои (Layers)** — организационные единицы для группировки террейнов
- **Система размещения объектов** — автоматическое распределение объектов по поверхности террейна

---

## 🎯 Возможности системы

### Создание террейнов
- **Процедурная генерация** на основе алгоритмов шума Perlin
- **Карты высот (Heightmaps)** — создание рельефа из изображений
- **Детерминированность** — воспроизводимые результаты с одинаковыми параметрами
- **Многоуровневая детализация** — комбинирование базового рельефа с точечными модификациями

### Типы рельефных форм
- **hill** — холмы и возвышенности
- **valley** — долины и понижения
- **crater** — кратеры с валами по краю
- **plateau** — плоские плато и террасы
- **ridge** — горные хребты и гряды
- **basin** — впадины и котловины

### Системы размещения
- **uniform** — равномерное случайное распределение
- **poisson** — распределение без скучивания
- **ring** — кольцевое размещение вокруг точки
- **gridJitter** — сетка с дрожанием

### Интеллектуальные фильтры
- **preferHeight** — предпочтение определённых высот
- **preferSlope** — учёт наклона поверхности
- **avoidOverlap** — избегание пересечений объектов

---

## 🔧 Способы работы с террейнами

### 1. 📱 Графический интерфейс (Scene Manager)
Интуитивный способ создания через пользовательский интерфейс:
- Scene Manager → "Добавить слой" → "Форма поверхности"
- Настройка параметров через формы
- Предпросмотр в реальном времени

### 2. 💻 Программное управление (Scripting Panel)
Полнофункциональное API для создания сложных ландшафтов:
- JavaScript API с методами `sceneApi`
- Полный контроль над всеми параметрами
- Возможность создания сложных алгоритмов генерации

> **Подробное руководство по программному созданию террейнов:** [terrain-in-scripting-panel.md](terrain-in-scripting-panel.md)

### 3. 🗂️ Создание из карт высот (Heightmaps)
Создание террейнов на основе изображений карт высот:
- **Загрузка файлов** — поддержка PNG, JPG, BMP форматов
- **Автоматическое масштабирование** — преобразование яркости пикселей в высоты
- **Настройка параметров** — контроль высоты, размеров и детализации
- **Предпросмотр** — визуализация результата перед применением

> **Пример использования:** Scene Manager → "Добавить слой" → "Форма поверхности" → "Карта высот"

### 4. 🗂️ Импорт геоданных
- Импорт из геодезических данных
- Преобразование топографических карт
- Интеграция с ГИС-системами

---

## 📋 Основные концепции

### Слои (Layers)
Террейны организованы в слои, каждый из которых:
- Имеет собственные настройки видимости
- Может содержать множество объектов
- Поддерживает независимое управление

### Мировые координаты
Система использует единые мировые координаты:
- **X, Z** — горизонтальная плоскость (земля)
- **Y** — вертикальная ось (высота)
- Единица измерения — метры

### Процедурная генерация
Все террейны создаются алгоритмически:
- **Seed (зерно)** — обеспечивает воспроизводимость результатов
- **Параметры шума** — контролируют характер базового рельефа
- **Операции модификации** — добавляют детали и формы

### Уровень моря и базовое смещение
- **`seaLevel`** — опорная высота воды (по умолчанию 0) в итоговой конфигурации террейна (`GfxTerrainConfig`).
- **`base.heightOffset`** (для Perlin) — смещение базового уровня рельефа. Отрицательные значения позволяют располагать базу ниже уровня моря, оставляя над водой только локальные возвышенности.
- **`edgeFade`** — затухание к краям ведётся к базовому уровню источника (т.е. к `base.heightOffset`), а не к жёсткому нулю.

### Выравнивание объектов
Система автоматически:
- Размещает объекты на поверхности террейна
- Учитывает высоту рельефа в каждой точке
- Поддерживает различные стратегии размещения

---

## 🚀 Быстрый старт

### Простой пример (процедурный)
1. Откройте Scene Manager
2. Выберите "Добавить слой" → "Форма поверхности"
3. Выберите тип "Рельефная поверхность"
4. Установите базовые параметры
5. Нажмите "Создать"

### Создание из карты высот
1. Подготовьте изображение карты высот (чёрно-белое или в оттенках серого)
2. Откройте Scene Manager → "Добавить слой" → "Форма поверхности"
3. Выберите тип "Карта высот"
4. Загрузите файл изображения
5. Настройте размеры и высоту террейна
6. Нажмите "Создать"

### Для разработчиков
```javascript
// Создание базового холмистого ландшафта
const result = await sceneApi.createProceduralLayer({
  world: { width: 200, depth: 200 },
  base: { seed: 42, amplitude: 8, octaveCount: 3, heightOffset: 0 },
  pool: { recipes: [] },
  seed: 42
}, { name: 'Мой первый террейн' })
```

---

## 📚 Связанная документация

- **[Создание террейнов в Scripting Panel](terrain-in-scripting-panel.md)** — полное руководство по программному API
- **[Типы данных террейнов](../../api/types/terrain.md)** — техническая документация по GfxTerrainConfig, GfxHeightSampler и процедурной генерации
- **[Scene Management](../scene-management/)** — общие принципы управления сценами
- **[Object Placement](../object-placement/)** — размещение объектов на террейнах

---

## ⚙️ Техническая архитектура

### Основные компоненты
- **TerrainGenerator** — генерация базового рельефа
- **OperationPool** — управление операциями модификации
- **PlacementEngine** — системы размещения объектов
- **LayerManager** — управление слоями сцены

### Производительность
- Оптимизированные алгоритмы для больших террейнов
- Ленивые вычисления для улучшения отзывчивости
- Кэширование результатов генерации

### Расширяемость
- Модульная архитектура для добавления новых типов операций
- API для создания пользовательских систем размещения
- Поддержка плагинов для специализированных алгоритмов

---

*Система террейнов — это основа для создания реалистичных и разнообразных ландшафтов в ваших 3D-сценах. Начните с простых примеров и постепенно осваивайте более сложные возможности.*
