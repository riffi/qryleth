# Фаза 6: Обновление утилит и хелперов

## Обязательная ссылка на инструкции
При выполнении фазы агент сверялся с требованиями в [agent-tasks.md](../../../docs/development/workflows/agent-tasks.md)

## Измененные файлы
- `src/entities/primitive/model/icons.ts`
- `src/entities/primitive/model/names.ts`

## Описание выполненной работы
- Функция `getPrimitiveIcon` теперь принимает либо тип примитива, либо сам объект. Добавлена проверка исчерпывающего перечисления типов через `assertNever`.
- Методы `getPrimitiveDisplayName` и `ensurePrimitiveNames` теперь используют обобщённые типы, что позволяет применять их к любым структурам с полями `type` и `name`.

## Статус проекта после фазы
- ✅ Утилиты работают с новой структурой `GfxPrimitive`.
- ✅ Проект компилируется.

## Новый контекст для будущих фаз
- Дополнительная типобезопасность при работе со вспомогательными функциями.

## Статус
**Выполнено** ✅

Дата выполнения: 2025-07-28
