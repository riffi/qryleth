/**
 * Хук-заглушка для будущей логики Play‑режима (горячие клавиши, подсказки и т.п.).
 * На текущем этапе остаётся презентационной: конкретная логика управляется виджетом/scene store.
 */
export function usePlayMode() {
  return {}
}

