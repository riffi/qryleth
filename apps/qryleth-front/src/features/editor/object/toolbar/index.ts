/**
 * Тулбары редактора объекта — целевой неймспейс `@/features/editor/object/toolbar`.
 *
 * В рамках миграции 027 исходники перенесены сюда физически.
 * Старый путь `@/features/object-toolbar` реэкспортирует эти компоненты на переходный период.
 */

export { LeftToolbar } from './LeftToolbar'
export { RightToolbar } from './RightToolbar'
